rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{id} {
      allow create: if request.resource.data.keys().hasAll(['name', 'phone', 'items', 'sum']);
      allow read, update, delete: if false;
    }
  }
}
